<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"      
 "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">
<!-- namespace:指定当前配置文件是给哪个接口文件中的抽象方法配置sql语句的 -->
<mapper namespace="cn.tedu.jmstore.mapper.AddressMapper">

	<!-- 新增收货地址 -->
	<!-- Integer saveAddress(Address address) -->
	<insert id="saveAddress"
		useGeneratedKeys="true"
		keyProperty="aid">
		INSERT INTO
			t_address(
			uid, name,
			province_name, province_code,
			city_name, city_code,
			area_name, area_code,
			zip, address,
			phone, tel,
			tag, is_default,
			created_user, created_time,
			modified_user, modified_time
		)VALUES(
			#{uid}, #{name},
			#{provinceName}, #{provinceCode},
			#{cityName}, #{cityCode},
			#{areaName}, #{areaCode},
			#{zip}, #{address},
			#{phone}, #{tel},
			#{tag}, #{isDefault},
			#{createdUser}, #{createdTime},
			#{modifiedUser}, #{modifiedTime}
		)
	</insert>

	<!-- 查询用户收货地址条数 -->
	<!-- Integer countByUid(Integer uid) -->
	<select id="countByUid"
		resultType="java.lang.Integer">
		SELECT
			count(*)
		FROM
			t_address
		WHERE
			uid=#{uid}
	</select>
	
	<select id="findByUid"
	    resultType="cn.tedu.jmstore.entity.Address">
	    select
	        aid,uid,name,
	        province_name as provinceName,
	        city_name as cityName,
	        area_name as areaName,
	        zip,address,phone,tel,tag,is_default as isDefault
	    from
	        t_address
	    where 
	        uid=#{uid} 
	    order by 
	        is_default desc, 
	        modified_time desc;
	</select>
</mapper>
