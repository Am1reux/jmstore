<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"      
 "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">
<!-- namespace:指定当前配置文件是给哪个接口文件中的抽象方法配置sql语句的 -->
<mapper namespace="cn.tedu.jmstore.mapper.DistrictMapper">

	<!-- 根据parent查子级地区信息 -->
	<!-- District findByParent(String parent) -->
	<select id="findByParent" 
	resultType="cn.tedu.jmstore.entity.District">
	    SELECT
	        id, parent,
	        code, name
	    FROM
	        t_dict_district
	    WHERE
	        parent=#{parent}
	</select>
	
	<!-- 根据code查询地区信息 -->
	<!-- District findByCode(String code) -->
	<select id="findByCode"
	resultType="cn.tedu.jmstore.entity.District">
	    SELECT
	        id, parent,
	        code, name
	    FROM
	        t_dict_district
	    WHERE
	        code=#{code}
	</select>
</mapper>
